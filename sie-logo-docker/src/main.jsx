import React from 'react'
import ReactDOM from 'react-dom/client'
import SIECubeLogo from './SIECubeLogo'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <div style={{ transform: 'scale(1.5)' }}>
            <SIECubeLogo />
        </div>
    </React.StrictMode>,
)
